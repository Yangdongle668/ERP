# 本地快速体验：无需安装 MySQL。数据保存在 ./data/erp-h2 文件中。
spring:
  datasource:
    url: jdbc:h2:file:./data/erp-h2;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;NON_KEYWORDS=VALUE,YEAR,MONTH
    username: sa
    password:
