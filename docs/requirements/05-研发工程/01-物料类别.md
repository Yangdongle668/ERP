# 05-01 物料类别

## 1. 功能说明

物料的多级分类，如“电子料 / IC”“FPC”“包材 / 纸箱”。类别用于：

1. 物料编码前缀（编码规则变量 `{categoryPrefix}`）；
2. 新建物料时带出默认属性（物料类型、单位、批次管理、是否来料检验等）；
3. 仓库模块按类别确定默认仓（类别 → 默认仓的映射在仓库模块维护）；
4. 查询、统计、报表分组。

- 使用者：数据管理员
- 优先级：P0
- 物料只能挂在**末级类别**上。

## 2. 数据表

### eng_material_category

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| parent_id | id | 否 | | 上级类别 |
| code | str(16) | 是 | | 类别编码，唯一，大写字母数字 |
| name | str(64) | 是 | | 名称 |
| code_prefix | str(8) | 是 | | 物料编码前缀，大写字母数字；默认等于类别编码的前 8 位。不同类别可以共用前缀 |
| default_material_type | enum(RAW/SEMI_FINISHED/FINISHED/PACKAGING/AUXILIARY/PHANTOM) | 是 | | 新建物料默认类型 |
| default_base_uom | str(16) | 否 | | 默认基本单位 |
| default_tracking | enum(NONE/BATCH/SERIAL) | 是 | NONE | 默认库存管理方式 |
| default_iqc_required | bool | 是 | 1 | 默认是否来料检验 |
| default_shelf_life_days | int | 否 | | 默认保质期（天） |
| path | str(256) | 是 | | 祖先路径 |
| level | int | 是 | | 层级，最多 5 级 |
| sort | int | 是 | 0 | |
| status | enum(ENABLED/DISABLED) | 是 | ENABLED | |
| remark | str(256) | 否 | | |

唯一：`uk_code(code)`。

### 初始数据

| 编码 | 名称 | 上级 | 前缀 | 默认类型 | 默认单位 | 批次 | 来料检 |
|---|---|---|---|---|---|---|---|
| RAW | 原材料 | — | RAW | 原材料 | PCS | 批次 | 是 |
| FPC | FPC | — | FPC | 原材料 | PCS | 批次 | 是 |
| ELEC | 电子料 | — | ELEC | 原材料 | PCS | 批次 | 是 |
| PKG | 包材 | — | PKG | 包材 | PCS | 不管理 | 是 |
| AUX | 辅料 | — | AUX | 辅料 | PCS | 不管理 | 否 |
| SEMI | 半成品 | — | SF | 半成品 | PCS | 批次 | 否 |
| FG | 成品 | — | FG | 成品 | PCS | 批次 | 否 |

（与仓库模块的 10 类仓对应：原材料仓、FPC 仓、电子料仓、包材仓、辅料仓、半成品仓、成品仓。）

## 3. 页面

### 3.1 物料类别（T6 树形表格）

路由 `/engineering/category`，权限 `eng:category:query`。

**查询条件**：名称/编码（模糊，命中节点及祖先显示）、状态。

**列表列**

| 列 | 字段 | 宽度 | 说明 |
|---|---|---|---|
| 名称 | name | 240 | 树形列 |
| 编码 | code | 110 | |
| 编码前缀 | code_prefix | 90 | |
| 默认类型 | default_material_type | 90 | |
| 默认单位 | default_base_uom | 80 | |
| 库存管理 | default_tracking | 90 | 不管理/批次/序列号 |
| 来料检验 | default_iqc_required | 80 | 是/否 |
| 物料数 | — | 80 | 本类别（含下级）启用物料数，点击跳转物料列表按该类别筛选 |
| 状态 | status | 70 | |
| 操作 | — | 200 | 新增下级、编辑、更多（停用/启用、删除） |

工具栏：[新建一级类别]（`eng:category:create`）、展开全部/收起全部。

### 3.2 类别表单（T2 大弹窗双列）

| 字段 | 控件 | 必填 | 默认 | 可编辑 | 校验 |
|---|---|---|---|---|---|
| 上级类别 | 树选择 | 否 | 触发行 | 始终 | R02、R03 |
| 编码 | 输入框 | 是 | | 未被物料使用时 | 1～16 位大写字母数字；R01 |
| 名称 | 输入框 | 是 | | 始终 | ≤64；同级不重复 |
| 编码前缀 | 输入框 | 是 | 编码前 8 位 | 始终 | 1～8 位大写字母数字；修改提示“只影响之后新建的物料” |
| 默认物料类型 | 下拉 | 是 | 继承上级 | 始终 | |
| 默认基本单位 | UomSelect | 否 | 继承上级 | 始终 | |
| 默认库存管理 | 单选 | 是 | 继承上级 | 始终 | |
| 默认来料检验 | 开关 | 是 | 继承上级 | 始终 | |
| 默认保质期（天） | 数字框 | 否 | 继承上级 | 始终 | 1～3650 |
| 排序 | 数字框 | 是 | 同级最大 + 10 | 始终 | |
| 备注 | 文本域 | 否 | | 始终 | |

## 4. 状态与流转

启用 ⇄ 停用。停用的类别不能在新建物料时选择，其下已有物料不受影响。

## 5. 业务规则

| 编号 | 触发 | 规则 | 提示原文 |
|---|---|---|---|
| ENG-CAT-R01 | 保存 | 编码唯一 | `类别编码「{code}」已存在` |
| ENG-CAT-R02 | 保存 | 上级不能是自己或下级 | `上级类别不能是自己或自己的下级` |
| ENG-CAT-R03 | 保存 | 最多 5 级 | `物料类别不能超过 5 级` |
| ENG-CAT-R04 | 新增下级 | 已挂有物料的类别不能再新增下级（物料只能挂在末级） | `该类别下已有物料，不能新增下级类别` |
| ENG-CAT-R05 | 删除 | 有下级或有物料（含停用）时不能删除 | `该类别下有下级类别或物料，不能删除` |
| ENG-CAT-R06 | 停用 | 有启用的下级类别时不能停用 | `请先停用下级类别` |
| ENG-CAT-R07 | 修改编码 | 已被物料使用的类别编码不能修改 | `该类别已被物料使用，不能修改编码` |

## 6. 接口

| 方法 | 路径 | 权限 |
|---|---|---|
| GET | /engineering/categories/tree | `eng:category:query` |
| GET | /engineering/categories/simple-tree | 登录即可（启用类别，供选择器） |
| GET / POST / PUT / DELETE | /engineering/categories[/{id}] | query / create / update / delete |
| POST | /engineering/categories/{id}/enable、/disable | update |

## 7. 验收用例

| 编号 | 前置条件 | 操作 | 预期结果 |
|---|---|---|---|
| ENG-CAT-T01 | ELEC 下无物料 | 新增下级 IC（前缀 IC） | 成功；默认属性继承 ELEC |
| ENG-CAT-T02 | ELEC 下已有物料 | 新增下级 | 提示“该类别下已有物料，不能新增下级类别” |
| ENG-CAT-T03 | IC 前缀为 IC | 在 IC 下新建物料 | 物料编码 IC00001 |
| ENG-CAT-T04 | FPC 下有物料 | 删除 FPC | 提示“该类别下有下级类别或物料，不能删除” |
